TODO list.

+  Time synchronization.
   After commissioning initiates TimeSync if it has not done yet or time elapsed from last sync more then 24 hours
   

+  When the device recives the read Battery attributes it has to update the values before sends response.
   Using AthorizeAccess flag of atributes.
   
+ Initiate Comissioning procedure after reboot if device was connected to Network

+  Long press button (5 seconds) to initiate Comissioning or to deactivate it.
   Status LED blinks slowly after 5 sec it starts to blink rapidply and if release the button than Status LED sets ON and Comissioning procedure is activated
   
+  Long press button (10 seconds) to reset device to the Factory New State.
   Status LED blinks rapidly and after 10 seconds all LEDs set ON that indicates that Reset to FN is initiated.

+  Update WorkingHours attribute every Hour
   update to function osal_GetSystemClockSec

+  Move the attribute processing algorithms to *_data.c file.

+ Reporting
  update function CfgReporting. It must add attributes into reporting list
  and send CfgReortingRsp.
  It is realized by adding #define ZCL_REPORT_DESTINATION_DEVICE
  
+ "CONST" code memory constant allocation
  "const" XDATA flash memory constant allocation
     
+ OSAL_Timers.c
  Added function uint32 osal_GetSystemClockSec()
  which return time elapsed since last boot in seconds
   
+ Write and restore the attributes to-from NV
  + Queue 10 items length
  + NV item ID, Data len, Pointer to Data
  + function NVItemQueueAdd. If item is already in queue than it doesnt add an item to queue
  + VAULES1 and 2 save without change chacking

+ Diagnostic cluster.
   +Last reboot reason.
   +Time since Last boot  = Current time of device - Time of first sync
   +NV items write counter
   +NV items write status word
   +HEAP memory statistics.
   +Send Diag cluster attributes every meter report period when zapp_DiagReport is 1, by default it is FALSE
   +Send Diag every 24 hours if zapp_DiagReport is 2
  
+ PowerManager
    +Add time delay to enable PWRMGR_CONSERVE when press BUTTON (1 minutes)
    +Add time delay to enable PWRMGR_CONSERVE when send Read Time attributes and FirstTrasmission (5 seconds)
    
+ Parent Lost rejoin counter "zapp_ParentLostRejoinCounter"
  Increased the time delay to next rejoin attempt with each rejoin fail (min 30 seconds, max 30 minutes)
   
+ Add "Transmissions" with the desired or missed type of trasmissions
  All SendReport calls were moved to special event (ZAPP_EVT_SENDCMD) and calls from DefaultRsp and ZAPP_EVT_UPDATE
  
+ Add ErasedPages and WrittenBytes to the OSAL_Nv.c and new functions:
  uint16 osal_nv_get_erasedpages()
  uint16 osal_nv_get_writtenbytes()
  
- Send alarm when Low Battery, Reboot, Flash Write Error, or may be send alarms through Diagnostic
  + added send alarm "General HW fault" and diag NV attributes if there was flash write error
  + added send alarm "Check meter" when device connects to network after reboot
  + added server alarm cluster
  - add send Bettary Low alarm

-  Switch debug messages to DebugTrace.h
  

    